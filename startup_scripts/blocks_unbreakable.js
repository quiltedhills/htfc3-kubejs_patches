// priority: 0

onEvent('block.modification', event => {
	event.modify('hardrock_samples:amethyst', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:bismuthinite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:bituminous_coal', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:borax', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:cassiterite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:cinnabar', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:copper', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:cryolite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:diamond_sample', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:emerald_sample', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:garnierite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:golds', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:graphite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:gypsum', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:halite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:hematite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:koalinite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:lapislazuli', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:lignite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:limonite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:magnetite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:malachite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:saltpeter', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:silver', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:sphalerite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:sulfur', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:sylvite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:tetrahedrite', block => {
	  block.destroySpeed = -1
	})
  event.modify('hardrock_samples:vulcanic_sulfur', block => {
	  block.destroySpeed = -1
	})
})
